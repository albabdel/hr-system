{
  "name": "hr-system",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "turbo run dev --parallel -- --port 9002 --hostname 0.0.0.0",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "2.5.8"
  }
}
