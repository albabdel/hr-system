import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function PayrollPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Payroll</CardTitle>
        <CardDescription>Manage payroll runs and view payslips.</CardHeader>
      </CardHeader>
    </Card>
  );
}
